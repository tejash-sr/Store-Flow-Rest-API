$kind: http-request
name: 'GET /actuator/health'
method: GET
url: '{{base_url}}/actuator/health'
order: 1000
headers:
  - key: Accept
    value: application/json
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      // ===== STATUS CODE VALIDATION =====
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      // ===== PERFORMANCE VALIDATION =====
      pm.test("Response time is below 2000ms", function () {
          pm.expect(pm.response.responseTime).to.be.below(2000);
      });

      // ===== CONTENT-TYPE VALIDATION =====
      pm.test("Content-Type header is application/json", function () {
          pm.response.to.have.header("Content-Type");
          pm.expect(pm.response.headers.get("Content-Type")).to.include("application/json");
      });

      // ===== RESPONSE BODY IS VALID JSON =====
      pm.test("Response body is valid JSON", function () {
          pm.response.to.be.json;
      });

      const jsonData = pm.response.json();

      // ===== STATUS FIELD VALIDATIONS =====
      pm.test("Response has 'status' field", function () {
          pm.expect(jsonData).to.have.property("status");
      });

      pm.test("'status' field equals 'UP'", function () {
          pm.expect(jsonData.status).to.eql("UP");
      });

      // ===== SPRING BOOT ACTUATOR FORMAT VALIDATION =====
      pm.test("Response body structure is valid Spring Boot Actuator format", function () {
          // Spring Boot Actuator /actuator/health returns at minimum a "status" field
          pm.expect(jsonData.status).to.be.a("string");
          // Status should be one of the standard Actuator health statuses
          const validStatuses = ["UP", "DOWN", "OUT_OF_SERVICE", "UNKNOWN"];
          pm.expect(validStatuses).to.include(jsonData.status);
      });

      pm.test("'status' field is a string", function () {
          pm.expect(jsonData.status).to.be.a("string");
      });

      // ===== ACTUATOR ENDPOINT IS PUBLICLY ACCESSIBLE =====
      pm.test("Actuator health endpoint is publicly accessible (no 401/403)", function () {
          pm.expect(pm.response.code).to.not.eql(401);
          pm.expect(pm.response.code).to.not.eql(403);
      });

      // ===== X-TRACE-ID HEADER (from RequestLoggingFilter) =====
      pm.test("Response includes X-Trace-Id header", function () {
          pm.response.to.have.header("X-Trace-Id");
      });

      pm.test("X-Trace-Id header is a valid UUID format", function () {
          const traceId = pm.response.headers.get("X-Trace-Id");
          pm.expect(traceId).to.match(/^[0-9a-f\-]+$/);
      });

      // ===== NEGATIVE VALIDATIONS =====
      pm.test("Response does NOT contain 'error' field at root level", function () {
          pm.expect(jsonData).to.not.have.property("error");
      });

      pm.test("Response does NOT contain 'message' field at root level", function () {
          pm.expect(jsonData).to.not.have.property("message");
      });
examples: './.resources/GET_actuator_health.resources/examples'
