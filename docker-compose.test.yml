# DEPRECATED: This file is no longer used.
# Testcontainers automatically manages PostgreSQL containers for all tests.
# 
# To run tests:
#   ./mvnw clean test
#
# Testcontainers will:
# 1. Check if Docker is available
# 2. If available: spin up PostgreSQL containers per test class
# 3. If unavailable: gracefully skip Testcontainers tests (disabledWithoutDocker=true)
#
# No manual docker-compose invocation needed.

